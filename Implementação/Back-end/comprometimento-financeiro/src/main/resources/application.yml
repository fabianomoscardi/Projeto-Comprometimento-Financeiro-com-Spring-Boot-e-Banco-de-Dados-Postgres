spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/comprometimento-financeiro-db?currentSchema=debug
    password: teste
    username: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop